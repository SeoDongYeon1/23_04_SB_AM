<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.KoreaIT.sdy.demo.repository.ReactionRepository">

	<insert id="GoodPoint">
		INSERT INTO reactionPoint
		SET regDate = NOW(),
		updateDate = NOW(),
		memberId = #{memberId},
		relTypeCode = 'article',
		relId = #{id},
		`point` = 1;
	</insert>
	
	<select id="getArticleLikeCount" resultType="int">
		SELECT SUM(IF(`point` &gt; 0, `point`, 0))
		FROM reactionPoint
		WHERE relId = #{id}
	</select>
	
	<select id="checkMember" resultType="int">
		SELECT IFNULL(SUM(R.`point`), 0) AS 'extra__sumReactionPoint',
		IFNULL(SUM(IF(R.`point` &gt; 0, R.`point`, 0)),0) AS 'extra__goodReactionPoint',
		IFNULL(SUM(IF(R.`point` &lt; 0, R.`point`, 0)),0) AS 'extra__badReactionPoint'
		FROM reactionPoint r
		WHERE memberId = #{memberId} AND relTypeCode = 'article' AND relId = #{id}
		AND memberId IS NOT NULL AND relId IS NOT NULL
		GROUP BY relId;
	</select>
	
	
</mapper>